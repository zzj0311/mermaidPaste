// Renderer is fully trusted (it runs our bundled mermaid code, never loads
// remote content), so we intentionally keep the preload minimal. Kept as a
// file to make future contextBridge wiring trivial.
export {};
